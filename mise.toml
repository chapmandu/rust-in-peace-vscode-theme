# Toolchain pins — local dev and CI (jdx/mise-action) install from here.
[tools]
node = "26" # vsce, prettier, fallow
uv = "0.11" # Python + script dependencies (see pyproject.toml)
just = "1" # task runner (justfile)
