# rust-in-peace-dawn-patrol — a Helix theme from the Megadeth "Rust in Peace" cover palette.
# Generated from the src/ palettes by scripts/targets/helix.py (just build-themes).
# Do not edit by hand: edit the palette and rebuild.
inherits = "tokyonight_day"

# Syntax roles mirror the rust-in-peace VSCode theme (original structure):
#   keywords / operators / delimiters / tags = electric tube blue
#   functions / decorators = glow green      strings  = logo gold
#   types / parameters     = logo orange     constants = violet
#   builtins               = sky cyan        comments  = muted blue
# UI structure comes from the inherited base theme. Every colour in
# [palette] derives from the shared source of truth: identity slots map
# straight to it (diffs and diagnostics follow the VSCode theme's UI
# elements), Helix-only shades via declared mix/adjust formulas.

keyword = { fg = "aqua" }
"keyword.control" = { fg = "aqua" }
"keyword.control.import" = { fg = "aqua" }
"keyword.control.return" = { fg = "aqua" }
"keyword.function" = { fg = "aqua" }
"keyword.operator" = { fg = "aqua" }
"keyword.directive" = { fg = "aqua" }
operator = { fg = "aqua" }
punctuation = { fg = "fg" }
"punctuation.delimiter" = { fg = "aqua" }
tag = { fg = "aqua" }
"constant.character.escape" = { fg = "aqua" }

function = { fg = "light-green" }
"function.builtin" = { fg = "cyan" }
"function.macro" = { fg = "light-green" }
attribute = { fg = "light-green", modifiers = ["italic"] }

string = { fg = "yellow" }
"string.regexp" = { fg = "yellow" }

type = { fg = "orange", modifiers = ["italic"] }
"type.builtin" = { fg = "orange", modifiers = ["italic"] }
constructor = { fg = "orange" }
"variable.parameter" = { fg = "orange", modifiers = ["italic"] }

constant = { fg = "purple" }
"constant.builtin" = { fg = "purple" }
"variable.builtin" = { fg = "cyan", modifiers = ["italic"] }
"variable.other.member" = { fg = "fg" }
namespace = { fg = "fg" }

"comment.block.documentation" = { fg = "comment", modifiers = ["italic"] }
"comment.line.documentation" = { fg = "comment", modifiers = ["italic"] }

"markup.heading" = { fg = "purple", modifiers = ["bold"] }
"markup.link" = { fg = "cyan", underline = { style = "line" } }
"markup.list" = { fg = "cyan" }
"markup.raw" = { fg = "light-green" }
"markup.quote" = { fg = "yellow", modifiers = ["italic"] }

"diff.plus" = { fg = "light-green" }
"diff.minus" = { fg = "red" }
"diff.delta" = { fg = "orange" }

[palette]
red              = "#b03911" # rust / logo-edge red
orange           = "#9c4c07" # logo orange — types, parameters
yellow           = "#815a03" # logo gold — strings
light-green      = "#297112" # glowing hand green — functions
green            = "#316190" # pale tube glow (unused member slot)
aqua             = "#0b5ad9" # electric tube blue — keywords, operators
teal             = "#3c83bc" # slate blue — markup, hints
turquoise        = "#445272" # muted hangar blue
light-cyan       = "#0493f6" # ice glow
cyan             = "#0d66a1" # sky cyan — builtins
blue             = "#0765a6" # bright tube blue — UI accents, labels
purple           = "#712ae5" # softened violet — constants, headings
magenta          = "#b83d78" # sarcophagus pink — select mode
comment          = "#606e94" # muted blue shading
black            = "#ced8ee" # raised surface blue

add              = "#297112" # as VS Code gitDecoration.added
change           = "#0d66a1" # as VS Code gitDecoration.modified
delete           = "#b03911" # as VS Code gitDecoration.deleted

error            = "#b03911" # as VS Code editorError
info             = "#0d66a1" # as VS Code editorInfo
hint             = "#3c83bc"

fg               = "#2f3d5c" # pale blue highlight white
fg-dark          = "#4d5e82"
fg-gutter        = "#c5cee1" # whitespace marks — flattened editorWhitespace
fg-linenr        = "#606e94" # as VS Code editorLineNumber
fg-selected      = "#b9caee" # menu selection bg — as VS Code list selection
border           = "#92a5cf"
border-highlight = "#0b5ad9"
bg               = "#d9e1f2" # hangar deep blue
bg-inlay         = "#c9d5f0" # flattened list.focusBackground
bg-selection     = "#b9caee"
bg-menu          = "#bfcce8" # statusline, popups
bg-focus         = "#bbc9e8"
